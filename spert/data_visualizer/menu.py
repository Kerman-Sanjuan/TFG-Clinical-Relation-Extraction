import tkinter as tk
import os
import utils

if __name__ == '__main__':
    root = tk.Tk()
    root.geometry('400x400')
    root.title('ADR Interface')
    utils.center(root)
    root.mainloop() #Esto es lo que genera la interfaz grafica
 